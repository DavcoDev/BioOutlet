{% extends 'base.html.twig' %}

{% block body %}
<!-- Main Content -->
<div class="container">
    <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">

            {% for pro in producteurs %}

                <div class="post-preview">
                        <h2 class="post-title">
                            {{ pro.nomFerme }}
                        </h2>
                        <h3 class="post-subtitle">

                        </h3>
                    </a>
                    <p class="post-meta">Proposé par : {{ pro.nomProducteur }}
                        <a href="{{ path('producteur_show', { 'id': pro.id }) }}">Voir le producteur</a>
                       </p>
                </div>
            {% endfor %}
        </div>
    </div>

    <hr>
{% endblock %}


